
server:
  port: 8090
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active:
      - "qa"

build:
    version: "3.0"

loans:
  message: "Welcome to Benki loans related local APIs "
  contactDetails:
    name: "Dragos Lech - Developer"
    email: "dragos@benki.com"
  onCallSupport:
    - (412) 000-3491
    - (915) 000-1932